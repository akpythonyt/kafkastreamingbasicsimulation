rm -rf .git                 # reset any old git history in parent
git init
git add Kafkastreaming/
git commit -m "Fresh push: only Kafkastreaming folder"
git branch -M main
git remote add origin git@github.com:akpythonyt/kafkastreamingbasicsimulation.git
git push -u origin main --force